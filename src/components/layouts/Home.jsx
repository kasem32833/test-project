import React from 'react';
import Banner from '../Banner';
import Header from '../Header';

const Home = () => {
    return (
        <div>
            <Header></Header>
            <Banner></Banner>
        </div>
    );
};

export default Home;